## Professional dark-theme Data Analyst portfolio (React) with skills, projects, experience, and contact

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
